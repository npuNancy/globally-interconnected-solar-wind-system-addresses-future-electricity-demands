当前代码结果不符合我的预期， @solar_wind_optimization_viz.ipynb   最后一个 cell存在问题：从`Optimization_SG_2050_Res.h5` 中解出来的2050光伏数量1145和风电数量3325，与 2040的候选数量光伏=4324  风电=4294，不一致；此外，2040的光伏选中是2280，而2030的光伏候选反而是2334，也不一致。
```
SA_2030 候选: 光伏=2334  风电=2312
SC_2040 候选: 光伏=4324  风电=4294
SG_2050 候选: 光伏=2166  风电=6743
  2030: 光伏选中 1,146/2,334  风电选中 1,340/2,312  弃电率=12.1%  渗透率=81.9%
  2040: 光伏选中 2,280/4,324  风电选中 2,313/4,294  弃电率=2.1%  渗透率=73.0%
  2050: 光伏选中 1,145/2,166  风电选中 3,325/6,743  弃电率=7.0%  渗透率=86.2%
```
------------------------
创建一个新的nootbook python文件，
1、从下面三个帕累托优化结果中获取2030、2040、2050 年最优解场站位置：
```
"Resilience/Optimization_SA_2030_Res.h5"
"Resilience/Optimization_SC_2040_Res.h5"
"Resilience/Optimization_SG_2050_Res.h5"
```
2、从`Optimization/Opt_SC_2040_Sel.mat` 和 `Optimization/Opt_SG_2050_Sel.mat` 中获取确定的场站位置
3、解答我上面提出的问题
4、使用下面代码做可视化：
```python

# ── 绘图 ──
fig, (ax1, ax2) = plt.subplots(
    2,
    1,
    figsize=(16, 14),
    subplot_kw={"projection": ccrs.Robinson()},
)
setup_basemap(ax1)
setup_basemap(ax2)

for year in [2030, 2040, 2050]:
    sc = scenarios[year]
    mask_s = sc["solar_sel"] == 1
    mask_w = sc["wind_sel"] == 1
    ax1.scatter(
        sc["slon"][mask_s],
        sc["slat"][mask_s],
        s=3,
        # alpha=0.45,
        c=year_colors[year],
        transform=ccrs.PlateCarree(),
        label=f"{year} ({mask_s.sum():,})",
        rasterized=True,
    )
    ax2.scatter(
        sc["wlon"][mask_w],
        sc["wlat"][mask_w],
        s=3,
        # alpha=0.45,
        c=year_colors[year],
        transform=ccrs.PlateCarree(),
        label=f"{year} ({mask_w.sum():,})",
        rasterized=True,
    )

for ax, title in [
    (ax1, "2030 / 2040 / 2050 最优解 — 太阳能场站部署"),
    (ax2, "2030 / 2040 / 2050 最优解 — 风电场站部署"),
]:
    ax.legend(loc="lower left", fontsize=11, markerscale=5, framealpha=0.9, edgecolor="#888")
    ax.set_title(title, fontsize=13, pad=10)

plt.tight_layout()
plt.show()
```