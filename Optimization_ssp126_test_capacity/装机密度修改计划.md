# 修改单位面积装机容量 — 实施计划

## 背景

对比两篇论文代码后发现，当前项目的装机密度（光伏 74、陆上风电 2.7、海上风电 4.6 MW/km²）与第一篇论文（Paris Agreement）的参数差异显著。需要以 `Optimization_ssp126/` 为基础，创建 `Optimization_ssp126_test_capacity/`，采用第一篇论文的参数方案进行修改。

## 参数变更

| 类型 | 旧值 | 新值 | 性质 |
|---|---:|---:|---|
| 光伏 | 74 MW/km² | 161.9 × Ω(lat) × 0.15 MW/km² | 纬度依赖 |
| 陆上风电 | 2.7 MW/km² | **3.68** MW/km² | 常数 |
| 海上风电 | 4.6 MW/km² | **6.07** MW/km² | 常数 |

光伏密度公式中：
- **161.9** W/m² = 光伏组件标准功率密度（STC 条件）
- **Ω** = 地面覆盖率比 GCR = cos(β) / (cos(β) + sin(β)/tan(α_min))，其中 β 为面板倾角，α_min 为冬至正午太阳高度角
- **β** = 0.35396 × |lat| + 16.84775°（面板倾角幅度，南北半球对称；原始 CF 脚本为 `0.35396*lat + sign(lat)*16.84775`，GCR 计算取绝对值后等价）
- **α_min** = 90° − |lat| − 23.45°
- **FR** = 0.15（统一值。LUCC 面积数据已编码土地覆盖约束，空间分异 FR 会导致双重计算）

预期密度范围：赤道 ≈ 21.4 MW/km² → 高纬度（60°）≈ 3.1 MW/km²

---

## 步骤 1：复制目录

```bash
cp -r Optimization_ssp126/ Optimization_ssp126_test_capacity/
cd Optimization_ssp126_test_capacity/

# 删除密度依赖的预计算文件（运行时自动重新生成）
rm -f NonlConData.mat NonlConData2040.mat NonlConData2030.mat
rm -f SolarInd2050.mat WindInd2050.mat
rm -f OptData_SC_2050.mat
rm -f results/*.h5 results/*_Sel.mat
```

**不需删除**：TIF、CF 数据（.mat/.h5）、负荷数据 — 这些与密度无关。

---

## 步骤 2：新建 `compute_pv_density.m`

**文件**：`Optimization_ssp126_test_capacity/compute_pv_density.m`

输入：180×360 矩阵的线性索引向量（如 `solar_index = find(grids < 65536)`）
输出：每个格网的 PV 装机密度（MW/km²），列向量

核心逻辑：
```matlab
function pv_density = compute_pv_density(solar_index)
    FR = 0.15;
    rows = mod(solar_index - 1, 180) + 1;   % 180×360 矩阵的行号
    lat  = 90.5 - rows;                      % 纬度

    beta      = 0.35396 * abs(lat) + 16.84775;  % 倾角幅度（南北半球对称）
    alpha_min = 90 - abs(lat) - 23.45;                      % 冬至高度角
    alpha_min = max(alpha_min, 0.1);                        % 极夜保护

    beta_rad     = deg2rad(beta);
    alpha_min_rad = deg2rad(alpha_min);

    Omega = cos(beta_rad) ./ (cos(beta_rad) + sin(beta_rad) ./ tan(alpha_min_rad));
    pv_density = 161.9 * Omega * FR;
    pv_density = pv_density(:);
end
```

设计说明：
- FR 取统一 0.15：LUCC 面积 TIF 已包含土地覆盖筛选，空间分异 FR 会双重计算
- α_min 下界 0.1°：防止高纬度极夜时 tan(0) 除零
- sign(lat) 处理：赤道格网 lat=0.5（非精确零），无问题

---

## 步骤 3：修改 4 个文件的密度常量

### 3.1 `Optimization_SC_2050.m`

**风电（第 49-51 行）**：
```matlab
% 旧
wind_ins=(2.7*areas)/1000/1000;
wind_ins(landmask==1)=(4.6*areas(landmask==1))/1000/1000;
% 新
wind_ins=(3.68*areas)/1000/1000;
wind_ins(landmask==1)=(6.07*areas(landmask==1))/1000/1000;
```

**光伏（第 88-92 行）**：
```matlab
% 旧
solar_ins=zeros([13296,1]);
for ii=1:13296
    solar_ins(ii)=(74*areas(ii))/1000/1000;
    solar_gen(ii,:)=solar_gen(ii,:)*(74*areas(ii))/1000/1000;
end
% 新
pv_density = compute_pv_density(solar_index);
solar_ins=zeros([13296,1]);
for ii=1:13296
    solar_ins(ii)=(pv_density(ii)*areas(ii))/1000/1000;
    solar_gen(ii,:)=solar_gen(ii,:)*(pv_density(ii)*areas(ii))/1000/1000;
end
clear pv_density
```

### 3.2 `Optimization_SC_2040.m`

**风电（第 53-55 行）**：`2.7→3.68`，`4.6→6.07`
**光伏（第 80-84 行）**：`74→pv_density(ii)`，同上模式

### 3.3 `Optimization_SA_2030.m`

**风电（第 50-52 行）**：`2.7→3.68`，`4.6→6.07`
**光伏（第 77-81 行）**：`74→pv_density(ii)`，同上模式

### 3.4 `convert_h5_to_sel.m`

4 处修改：

1. **第 69-72 行**（光伏装机）：`74 → pv_density`
   ```matlab
   pv_density = compute_pv_density(solar_index_all);
   solar_ins = zeros(length(solar_index_all), 1);
   for ii = 1:length(solar_index_all)
       solar_ins(ii) = (pv_density(ii) * areas(ii)) / 1e6;
   end
   ```

2. **第 82-83 行**（光伏发电）：`74 → pv_density_f`
   ```matlab
   pv_density_f = compute_pv_density(solar_index_f);
   solar_gen_f = zeros(sum(tmp), 8760);
   for ii = 1:sum(tmp)
       solar_gen_f(ii,:) = res_CF_scaled(tmp_idx(ii), :) * (pv_density_f(ii) * areas_solar_f(ii)) / 1e6;
   end
   clear pv_density_f
   ```

3. **第 105 行**（陆上风电）：`2.7 → 3.68`
4. **第 106 行**（海上风电）：`4.6 → 6.07`

---

## 步骤 4：NonlConData 自动重新生成

**无需额外操作**。三个文件在运行时自动保存：

| 文件 | 保存行 | 保存内容 |
|---|---|---|
| `Optimization_SC_2050.m` 第 132 行 | `save NonlConData nonlcon_sel nonlcon_ins nonlsol nonlwin` |
| `Optimization_SC_2040.m` 第 112 行 | `save NonlConData2040 ...` |
| `Optimization_SA_2030.m` 第 109 行 | `save NonlConData2030 ...` |

`nonlcon_ins` = `all_ins`（由密度计算得来），旧文件删除后新值自动写入。

**以下文件无需修改**：
- `nonlcon2050/2040/2030.m` — 从 NonlConData*.mat 加载
- `OptFun_SC_Dispatch_2050/2040.m`、`OptFun_SA_Dispatch_2030.m` — 接收 ins_cap 参数
- 所有工具函数（geotiffread.m、readtif_custom.m、nansum.m 等）

---

## 步骤 5：验证

### 5.1 密度函数验证

在 `Optimization_ssp126_test_capacity/` 下运行：
```matlab
% 构造已知纬度的测试索引
test_rows = [1, 30, 60, 90, 120, 150, 180];  % lat = 89.5, 60.5, 30.5, 0.5, -29.5, -59.5, -89.5
pv_dens = compute_pv_density(test_rows);
fprintf('lat=%5.1f  density=%.2f MW/km^2\n', [90.5-test_rows; pv_dens']');
```

预期结果：
| 纬度 | 密度 (MW/km²) |
|---|---:|
| 89.5° | ~0.5（极地，Ω 很小） |
| 60.5° | ~3.1 |
| 30.5° | ~14.5 |
| 0.5° | ~21.4 |

**实际验证结果（2026-06-01）**：
| 纬度 | 实际密度 (MW/km²) |
|---|---:|
| 89.5° | 0.04 |
| 60.5° | 2.88 |
| 30.5° | 14.12 |
| 0.5° | 21.38 |
| -29.5° | 14.43 |
| -59.5° | 3.33 |
| -89.5° | 0.04 |

南北半球对称，赤道最大（21.38），极地最小（0.04），符合预期。

> **修正记录**：计划初版中 beta 公式为 `0.35396*|lat| + sign(lat)*16.84775`，对南半球会产生负的倾角导致 GCR 计算错误。
> 已修正为 `0.35396*|lat| + 16.84775`（倾角幅度，始终为正），与 CF 模拟脚本取绝对值后等价。

### 5.2 优化运行结果

完整流水线已运行完成（2050 → convert_h5_to_sel → 2040 → convert_h5_to_sel → 2030 → convert_h5_to_sel），结果汇总：

| 年份 | 来源 | 光伏 (GW) | 风电 (GW) | 合计 (GW) |
|---|---|---:|---:|---:|
| **2050** | 优化中位数 | 23,286 | 4,316 | 27,568 |
| | 优化均值 | 23,309 | 4,319 | 27,628 |
| | AR6 SSP1-26 | 5,799 | 6,418 | 12,217 |
| **2040** | 优化中位数 | 10,194 | 2,228 | 12,425 |
| | 优化均值 | 10,513 | 2,231 | 12,744 |
| | AR6 SSP1-26 | 3,318 | 4,443 | 7,761 |
| **2030** | 优化中位数 | 2,815 | 1,092 | 3,898 |
| | 优化均值 | 2,767 | 1,064 | 3,831 |
| | AR6 SSP1-26 | 1,172 | 2,358 | 3,530 |

与旧参数（74/2.7/4.6 MW/km²）对比（中位数）：

| 年份 | 类型 | 旧参数 | 新参数 | 变化 |
|---|---|---:|---:|---:|
| 2050 | 光伏 | 42,061 | 23,286 | -45% |
| 2050 | 风电 | 3,298 | 4,316 | +31% |
| 2040 | 光伏 | 16,284 | 10,194 | -37% |
| 2040 | 风电 | 1,737 | 2,228 | +28% |
| 2030 | 光伏 | 3,985 | 2,815 | -29% |
| 2030 | 风电 | 779 | 1,092 | +40% |

中位数与均值差异 <2%，Pareto 前沿分布对称。

### 5.3 完整流水线

运行顺序：2050 → convert_h5_to_sel(2050) → 2040 → convert_h5_to_sel(2040) → 2030 → convert_h5_to_sel(2030)

状态：**已完成**（2026-06-01）

---

## 修改文件清单

| 文件 | 操作 | 修改量 |
|---|---|---|
| `compute_pv_density.m` | 新建 | 约 25 行 |
| `Optimization_SC_2050.m` | 改风电（第49-51行）+ 光伏（第88-92行） | 2 处 |
| `Optimization_SC_2040.m` | 改风电（第53-55行）+ 光伏（第80-84行） | 2 处 |
| `Optimization_SA_2030.m` | 改风电（第50-52行）+ 光伏（第77-81行） | 2 处 |
| `convert_h5_to_sel.m` | 改光伏装机（第69-72）+ 光伏发电（第82-83）+ 风电（第105-106） | 4 处 |

**共修改 4 个现有文件 + 新建 1 个函数文件**。其余 13 个 .m 文件无需改动。

---

## 风险与注意事项

1. **光伏容量大幅下降**：新密度（3~21 MW/km²）远低于旧值 74，总装机潜力将显著缩减。这可能导致优化需要选中更多格网才能满足同样的需求
2. **筛选阈值**：`solar_ins > 0.001 TWp` 的阈值在新密度下更难达到，高纬度小面积格网可能被排除
3. **优化收敛**：密度变化改变决策变量的重要性分布，可能需要调整种群规模或迭代次数
4. **不需要额外数据**：所有计算基于现有 TIF 文件的纬度信息，不引入外部数据
